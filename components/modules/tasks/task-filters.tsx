import { Card } from "@/components/ui/card";

export function TaskFilters() {
  return (
    <Card>
      <h2 className="mb-3 text-sm font-semibold">Filtros</h2>
      <p className="text-sm text-foreground/70">Estado, fecha limite y prioridad.</p>
    </Card>
  );
}
